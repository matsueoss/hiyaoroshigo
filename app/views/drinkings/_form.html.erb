<%= form_for(drinking) do |f| %>
  <% if drinking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drinking.errors.count, "error") %> prohibited this drinking from being saved:</h2>

      <ul>
      <% drinking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
